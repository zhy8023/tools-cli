{
  "version": "0.0.1",
  "main": "bin/struct",
  "name": "test-cli",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "bin": {
    "struct": "bin/struct",
    "struct-init": "bin/struct-init"
  },
  "dependencies": {
    "chalk": "^2.3.0",
    "commander": "^2.11.0",
    "inquirer": "^3.3.0",
    "ora": "^1.3.0"
  },
  "devDependencies": {
    "metalsmith": "^2.3.0",
    "rimraf": "^2.6.2"
  }
}
